{
  "name": "vandy-rmp-integration",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "vite build --config vite.config.content.ts && vite build --config vite.config.sw.ts",
    "dev": "concurrently \"vite build --watch --config vite.config.content.ts\" \"vite build --watch --config vite.config.sw.ts\"",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "concurrently": "^8.2.2",
    "typescript": "^5.4.5",
    "vite": "^5.2.11"
  }
}
